version: '3'
services:
    # Mercury:
    #     image: milkyway_mercury:latest
        # build: .
    # Venus:
    #     image: milkyway_venus:latest
        # build: .
    earth:
        # image: milkyway_earth:latest
        build: Earth/.Depoyment
        volumes:
          - ./Earth/app/index.html:/usr/share/nginx/html/index.html
    # Mars:
    #     image: milkyway_mars:latest
        # build: .
    # Ceres:
    #     image: milkyway_ceres:latest
        # build: .
    jupiter:
        # image: milkyway_jupiter:latest
        build: Jupiter/.Depoyment
        volumes:
          - ./Jupiter/proxy/proxy.conf:/etc/nginx/conf.d/proxy.conf
        ports:
          - "81:80"
    # Saturn:
    #     image: milkyway_saturn:latest
        # build: .
    # Uranus:
    #     image: milkyway_uranus:latest
        # build: .
    # Neptune:
    #     image: milkyway_neptune:latest
        # build: .
    # Pluto:
    #     image: milkyway_pluto:latest
        # build: .
